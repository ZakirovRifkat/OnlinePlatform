export const STAND_GEOMETRY_ARGS: [number, number, number, number] = [
    1, 3, 1, 1024,
];

export const METAL_TEXTURE_TYPES = [
    "Color",
    "Displacement",
    "NormalDX",
    "Roughness",
    "Metalness",
] as const;

export const CLASSIC_UPDATE_DELAY_MS = 10;
export const SERVO_UPDATE_DELAY_MS = 20;
